name: Test Runner

on:
  push:
    branches:
      - 'piotr-stec/**'

jobs:
  test:
    name: Test Runner Functionality
    runs-on: arc-runner-set
    steps:
    - name: Print Debug Info
      run: echo "Runner is working!"
